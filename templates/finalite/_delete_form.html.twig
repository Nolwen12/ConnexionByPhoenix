<form method="post" action="{{ path('app_finalite_delete', {'id': finalite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ finalite.id) }}">
    <button class="btn">Delete</button>
</form>
