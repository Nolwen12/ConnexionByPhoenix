{% extends 'base.html.twig' %}

{% block title %}DemandeurEmploie{% endblock %}

{% block body %}
    <h1>DemandeurEmploie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ demandeur_emploie.id }}</td>
            </tr>
            <tr>
                <th>Cv</th>
                <td>{{ demandeur_emploie.cv }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_demandeur_emploie_index') }}">back to list</a>

    <a href="{{ path('app_demandeur_emploie_edit', {'id': demandeur_emploie.id}) }}">edit</a>

    {{ include('demandeur_emploie/_delete_form.html.twig') }}
{% endblock %}
