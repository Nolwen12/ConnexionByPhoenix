<form method="post" action="{{ path('app_demandeur_emploie_delete', {'id': demandeur_emploie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demandeur_emploie.id) }}">
    <button class="btn">Delete</button>
</form>
